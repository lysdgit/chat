CREATE TABLE IF	NOT EXISTS `chat` ( 
`id` VARCHAR ( 100 ) NOT NULL, 
`pwd` VARCHAR ( 40 ) NOT NULL, 
PRIMARY KEY ( `id` ) `id` VARCHAR ( 100 ) NOT NULL, 
`pwd` VARCHAR ( 40 ) NOT NULL, 
PRIMARY KEY ( `id` ) 
) ENGINE = INNODB DEFAULT CHARSET = utf8;

INSERT INTO chat ( id,pwd ) VALUES  (admin,admin);

select * from chat
) ENGINE = INNODB DEFAULT CHARSET = utf8;

INSERT INTO chat ( id,pwd ) VALUES  (admin,admin);

select * from chat
